language: node_js
node_js:
  - 'node'
before_install:
  - npm install -i check-build
  - npm install -i mocha

cache:
  directories:
    - "node_modules"

install:
  - npm install

script:
  - check-build
  - mocha --exit